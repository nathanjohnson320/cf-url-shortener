{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "$npm_execpath run build && wrangler pages deploy",
    "migrate": "npx wrangler d1 migrations apply url_shortener --local"
  },
  "dependencies": {
    "hono": "^4.4.13",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "@hono/vite-dev-server": "^0.13.0",
    "vite": "^5.2.12",
    "wrangler": "^3.57.2"
  }
}
